package com.syntax.class04;

public class N0_Notes1_23 {

	public static void main(String[] args) {
		
		/*
		 * Shorthand value: Int i=10; i+=100; //add 100 // +=, -=, *=, /=, %=
		 * System.out.println(i); //prints 110
		 * 
		 * Casting: widening and narrowing double d=10; //widening because it is an
		 * integer that is stored as a double System.out.println(d); //10.0 because it’s
		 * a double
		 * 
		 * double in=(int)10.99; //narrowing because you're forcing a double to be
		 * stored as an int System.out.println(in); //10 everything after the decimal
		 * gets cut off
		 * 
		 * Conditional statement: If statement Switch case
		 * 
		 * Nested if: if inside another if If (boolean condition) { //outer if Code a;
		 * If (boolean condition){ //nested if Code b; } } else { //means “if” condition
		 * was false Code for “else” block }
		 * 
		 * It is a good habit to finish the Outer if (all the way down to the “else”
		 * statement), BEFORE writing the Nested if.
		 * 
		 * 
		 * “else” cannot exist by itself. It must come with an “if” statement. “If” CAN
		 * exist by itself, but every “else” is dependent on an “if”.
		 * 
		 */
		
	}

}
